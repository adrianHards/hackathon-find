<h1>Confirmation</h1>
<div data-controller="facial-recognition">
  <% if @match == nil %>
  <% end %>
</div>

<%= @data["data"]["resultMessage"] %>
<%= number_to_percentage(@data["data"]["similarPercent"] * 100) %> match!